import { createSlice } from '@reduxjs/toolkit';

const mpSlice = createSlice({
    name: 'mpSlice',
    initialState: {},
    reducers: {}
});

export default mpSlice.reducer;
export const {
} = mpSlice.actions;
